# Team Workflow

- `main`: stable/demo-ready only.
- `develop`: shared integration branch.
- Each member works on a dedicated `feat/*` branch.
- Open small PRs into `develop`.
- Keep `develop` runnable every day.
- Use mock data instead of waiting on another member.
- Do not casually change shared contracts.
